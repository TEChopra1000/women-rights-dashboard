Stage, commit, and push the three core files to GitHub Pages.

Ask the user for a short commit message describing what changed. Then:

1. Run `git status` to confirm which files are modified.
2. Stage only the modified core files (index.html, styles.css, script.js, and CLAUDE.md if changed). Do NOT use `git add -A` or `git add .` — stage files by name.
3. Commit with the user's message. No co-authorship lines.
4. Run `git push origin main`.
5. Print the live URL: https://techopra1000.github.io/women-rights-dashboard/
6. Tell the user GitHub Pages typically rebuilds within 1–2 minutes.
